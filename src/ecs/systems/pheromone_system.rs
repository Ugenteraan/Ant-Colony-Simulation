//Pheromone-related logics.
mod config;
use crate::ecs::entity_data::EntityData;
use crate::resources::world::World;
use crate::utils;

//pub fn find_nearby_pheromones(world: &World, position: Vec2, direction: Vec2) -> Option<EntityData> {

//   let radius: usize = config::PHEROMONE_SEARCH_RADIUS;
//  let half_angle: u16 = config::PHEROMONE_SEARCH_CONE_ANGLE;
//
//   let (x, y) = utils::world_to_grid(position);

//}
